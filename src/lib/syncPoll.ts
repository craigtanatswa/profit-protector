/** Owner fallback poll when Realtime is quiet (`useOwnerSalesRealtimeSync`). */
export const FOREGROUND_INVENTORY_POLL_MS = 5_000

/** Shopkeeper foreground fallback poll when Realtime is quiet — backs up `business_staff_signals`. */
export const SHOPKEEPER_FOREGROUND_POLL_MS = 3_000
